%p= link_to "Dodaj Produkt", new_admin_product_path
Lista produktów:

- @products.each do |p|
  %p= link_to p.name, product_path(p.id)
  = 'cena: ' + p.price.to_s
  = link_to "Edytuj", edit_admin_product_path(p.id)
  = link_to "Usuń", admin_product_path(p.id), :method => 'delete'
